// Type-only check for the generated TypeScript definitions (index.d.ts), run
// via `npm run test:types` (tsc --noEmit). This file has no runtime
// assertions -- a type error here is the test failure.
//
// index.d.ts is currently empty (see docs/INTEGRATION.md): it's generated
// from the Rust types via ts-rs/schemars (the `bindings` feature), which
// hasn't been run yet because src/napi_bindings.rs has no exports. Once it
// does and index.d.ts is regenerated, replace the placeholder below with real
// imports from "narrative-lens" and assert their shape at the type level.

export {};
